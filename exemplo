react native 

import React from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet, ScrollView } from 'react-native';
import { Ionicons } from '@expo/vector-icons';

export default function App() {
  return (
    <View style={styles.container}>
      {/* Logo */}
      <View style={styles.logoBox}>
        <Text style={styles.logoText}>LOGO EMPRESA</Text>
      </View>

      {/* Saudação */}
      <Text style={styles.greeting}>Olá, Nome do Usuário! 👋</Text>

      {/* Campo de busca */}
      <View style={styles.searchBox}>
        <Ionicons name="search" size={20} color="#555" style={{ marginRight: 5 }} />
        <TextInput placeholder="Buscar entrevistas..." style={{ flex: 1 }} />
      </View>

      {/* Botão nova entrevista */}
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Começar nova entrevista</Text>
      </TouchableOpacity>

      {/* Minhas Entrevistas */}
      <Text style={styles.sectionTitle}>Minhas Entrevistas</Text>

      <View style={styles.card}>
        <Text style={styles.cardTitle}>Entrevista 1</Text>
        <Text style={styles.cardSubtitle}>Feedback disponível ✓</Text>
      </View>

      <View style={styles.card}>
        <Text style={styles.cardTitle}>Entrevista 2</Text>
        <Text style={styles.cardSubtitle}>Em análise pela IA ⏳</Text>
      </View>

      {/* Feedback Inteligente */}
      <Text style={styles.sectionTitle}>🤖 Feedback Inteligente</Text>

      {/* Painel de Progresso */}
      <Text style={styles.sectionSubtitle}>Painel de Progresso</Text>
      <View style={styles.progressContainer}>
        <View style={styles.progressItem}>
          <Text>Comunicação</Text>
          <View style={styles.progressBar}>
            <View style={[styles.progressFill, { width: '85%' }]} />
          </View>
          <Text>85%</Text>
        </View>

        <View style={styles.progressItem}>
          <Text>Confiança</Text>
          <View style={styles.progressBar}>
            <View style={[styles.progressFill, { width: '78%' }]} />
          </View>
          <Text>78%</Text>
        </View>

        <View style={styles.progressItem}>
          <Text>Clareza</Text>
          <View style={styles.progressBar}>
            <View style={[styles.progressFill, { width: '92%' }]} />
          </View>
          <Text>92%</Text>
        </View>
      </View>

      {/* Barra de Navegação */}
      <View style={styles.bottomNav}>
        <View style={styles.navItem}>
          <Ionicons name="home" size={24} color="#000" />
          <Text>Início</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="time" size={24} color="#000" />
          <Text>Histórico</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="settings" size={24} color="#000" />
          <Text>Configurações</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="help-circle" size={24} color="#000" />
          <Text>Ajuda</Text>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#fff',
    paddingTop: 50
  },
  logoBox: {
    alignSelf: 'center',
    padding: 10,
    borderWidth: 1,
    borderRadius: 5
  },
  logoText: {
    fontWeight: 'bold'
  },
  greeting: {
    fontSize: 18,
    fontWeight: 'bold',
    marginVertical: 15
  },
  searchBox: {
    flexDirection: 'row',
    borderWidth: 1,
    borderRadius: 5,
    padding: 8,
    alignItems: 'center',
    marginBottom: 15
  },
  button: {
    backgroundColor: '#222',
    padding: 15,
    borderRadius: 8,
    alignItems: 'center',
    marginBottom: 20
  },
  buttonText: {
    color: '#fff',
    fontWeight: 'bold'
  },
  sectionTitle: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 8
  },
  sectionSubtitle: {
    fontSize: 14,
    fontWeight: 'bold',
    marginTop: 8
  },
  card: {
    borderWidth: 1,
    padding: 10,
    borderRadius: 8,
    marginBottom: 10
  },
  cardTitle: {
    fontWeight: 'bold'
  },
  cardSubtitle: {
    color: '#555'
  },
  progressContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 10
  },
  progressItem: {
    alignItems: 'center'
  },
  progressBar: {
    width: 70,
    height: 8,
    backgroundColor: '#ccc',
    borderRadius: 5,
    marginVertical: 5
  },
  progressFill: {
    height: 8,
    backgroundColor: '#222',
    borderRadius: 5
  },
  bottomNav: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    paddingVertical: 10,
    borderTopWidth: 1,
    marginTop: 'auto'
  },
  navItem: {
    alignItems: 'center'
  }
});


import React from "react";
import { View, Text, TextInput, TouchableOpacity, StyleSheet, ScrollView } from "react-native";
import { Ionicons } from "@expo/vector-icons";

export default function App() {
  return (
    <View style={styles.container}>
      <ScrollView contentContainerStyle={{ paddingBottom: 100 }}>
        {/* Logo */}
        <View style={styles.logoBox}>
          <Text style={styles.logoText}>LOGO EMPRESA</Text>
        </View>

        {/* Saudação */}
        <Text style={styles.greeting}>Olá, Nome do Usuário! 👋</Text>

        {/* Busca */}
        <View style={styles.searchBox}>
          <Ionicons name="search" size={20} color="#555" style={{ marginRight: 5 }} />
          <TextInput placeholder="Buscar entrevistas..." style={{ flex: 1 }} />
        </View>

        {/* Botão nova entrevista */}
        <TouchableOpacity style={styles.button}>
          <Text style={styles.buttonText}>Começar nova entrevista</Text>
        </TouchableOpacity>

        {/* Minhas Entrevistas */}
        <Text style={styles.sectionTitle}>Minhas Entrevistas</Text>

        <View style={styles.card}>
          <Text style={styles.cardTitle}>Entrevista 1</Text>
          <Text style={styles.cardSubtitle}>Feedback disponível ✓</Text>
        </View>

        <View style={styles.card}>
          <Text style={styles.cardTitle}>Entrevista 2</Text>
          <Text style={styles.cardSubtitle}>Em análise pela IA ⏳</Text>
        </View>

        {/* Feedback Inteligente */}
        <Text style={styles.sectionTitle}>🤖 Feedback Inteligente</Text>

        {/* Painel de Progresso */}
        <Text style={styles.sectionSubtitle}>Painel de Progresso</Text>

        <View style={styles.progressContainer}>
          <View style={styles.progressItem}>
            <Text>Comunicação</Text>
            <View style={styles.progressBar}>
              <View style={[styles.progressFill, { width: "85%" }]} />
            </View>
            <Text>85%</Text>
          </View>

          <View style={styles.progressItem}>
            <Text>Confiança</Text>
            <View style={styles.progressBar}>
              <View style={[styles.progressFill, { width: "78%" }]} />
            </View>
            <Text>78%</Text>
          </View>

          <View style={styles.progressItem}>
            <Text>Clareza</Text>
            <View style={styles.progressBar}>
              <View style={[styles.progressFill, { width: "92%" }]} />
            </View>
            <Text>92%</Text>
          </View>
        </View>
      </ScrollView>

      {/* Navegação inferior */}
      <View style={styles.bottomNav}>
        <View style={styles.navItem}>
          <Ionicons name="home" size={24} color="#000" />
          <Text>Início</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="time" size={24} color="#000" />
          <Text>Histórico</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="settings" size={24} color="#000" />
          <Text>Configurações</Text>
        </View>
        <View style={styles.navItem}>
          <Ionicons name="help-circle" size={24} color="#000" />
          <Text>Ajuda</Text>
        </View>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#fff", padding: 20, paddingTop: 50 },
  logoBox: {
    alignSelf: "center",
    padding: 10,
    borderWidth: 1,
    borderRadius: 5,
  },
  logoText: { fontWeight: "bold" },
  greeting: { fontSize: 18, fontWeight: "bold", marginVertical: 15 },
  searchBox: {
    flexDirection: "row",
    borderWidth: 1,
    borderRadius: 5,
    padding: 8,
    alignItems: "center",
    marginBottom: 15,
  },
  button: {
    backgroundColor: "#222",
    padding: 15,
    borderRadius: 8,
    alignItems: "center",
    marginBottom: 20,
  },
  buttonText: { color: "#fff", fontWeight: "bold" },
  sectionTitle: { fontSize: 16, fontWeight: "bold", marginBottom: 8 },
  sectionSubtitle: { fontSize: 14, fontWeight: "bold", marginTop: 8 },
  card: {
    borderWidth: 1,
    padding: 10,
    borderRadius: 8,
    marginBottom: 10,
  },
  cardTitle: { fontWeight: "bold" },
  cardSubtitle: { color: "#555" },
  progressContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginTop: 10,
  },
  progressItem: { alignItems: "center" },
  progressBar: {
    width: 70,
    height: 8,
    backgroundColor: "#ccc",
    borderRadius: 5,
    marginVertical: 5,
  },
  progressFill: {
    height: 8,
    backgroundColor: "#222",
    borderRadius: 5,
  },
  bottomNav: {
    flexDirection: "row",
    justifyContent: "space-around",
    paddingVertical: 10,
    borderTopWidth: 1,
    backgroundColor: "#fff",
    position: "absolute",
    bottom: 0,
    width: "100%",
  },
  navItem: { alignItems: "center" },
});
